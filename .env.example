# Kopieren nach ".env" und Wert eintragen (optional – ohne Key funktionieren
# nur die Fallback-Suchlinks in Stufe 3, siehe README). ".env" wird von Git
# ignoriert und niemals committet.
OUTDOORACTIVE_API_KEY=
